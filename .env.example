# Database (PostgreSQL)
# If you run Postgres via `docker compose up -d`, localhost:5432 will work from your host machine.
DATABASE_URL="postgresql://stockly:stockly_password@localhost:5432/stockly?schema=public"

# JWT used by API auth/session
JWT_SECRET="change-me-in-development"

# Optional
# NOTE: Public self-registration is disabled (admin-only provisioning). This flag is ignored.
# ALLOW_REGISTRATION="true"

# Multi-tenant: used when the client does not send a tenant identifier.
DEFAULT_TENANT_SLUG="default"

# Comma-separated list of allowed origins for cross-site login.
# Preferred name: CORS_ALLOWED_ORIGINS (ALLOWED_ORIGINS is accepted as an alias).
# CORS_ALLOWED_ORIGINS="http://localhost:3000"

# If running behind a reverse proxy / load balancer that sets x-forwarded-for.
TRUST_PROXY="false"

# Allowlist bootstrap mode: if true, the first ADMIN login (when allowlist is empty)
# seeds the allowlist with the current IP to avoid initial lockout.
# Recommended: set explicitly to false in production once allowlist is configured.
ALLOWLIST_BOOTSTRAP_ADMIN="false"

# Rate limit backend:
# - memory (default): per-instance only
# - db/postgres: shared across all app instances via Postgres table RateLimitBucket
# RATE_LIMIT_STORE="db"

# Optional structured logging verbosity
# LOG_LEVEL="info"

# Optional API base URL for the client
NEXT_PUBLIC_API_BASE_URL="/api"

# Optional: public base URL of the app (used to generate share/print links in some pages)
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: logo URL used in print views
# NEXT_PUBLIC_PRINT_LOGO_URL="https://example.com/logo.png"
