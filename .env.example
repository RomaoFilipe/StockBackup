# Database (PostgreSQL)
# If you run Postgres via `docker compose up -d`, localhost:5432 will work from your host machine.
DATABASE_URL="postgresql://stockly:stockly_password@localhost:5432/stockly?schema=public"

# JWT used by API auth/session
JWT_SECRET="change-me-in-development"

# Optional
ALLOW_REGISTRATION="true"
# Comma-separated list of allowed origins (useful when frontend/backend are split)
# ALLOWED_ORIGINS="http://localhost:3000"

# Optional API base URL for the client
NEXT_PUBLIC_API_BASE_URL="/api"
